# Housing Price Linear Regression

This repository provides a simple example of using **NumPy** and **Matplotlib** to perform a linear regression on housing prices based on square footage.

Run the example script:

```bash
python house_price_regression.py
```

The script will output the regression parameters and generate a plot `house_price_regression.png` showing the data points and the fitted line.
